extend type Query {
  users: [User!]!
  user(id: ID!): User
}

extend type Mutation {
  updateUser(id: ID!, input: UpdateUserInput!): User! @isAuthenticated
  deleteUser(id: ID!): String! @can(resource: "user", action: "delete")
}

type Role {
  id: ID!
  name: String!
}

type User {
  id: ID!
  name: String!
  email: String!
  roles: [Role!]!
}

input UpdateUserInput {
  name: String!
  email: String!
}
